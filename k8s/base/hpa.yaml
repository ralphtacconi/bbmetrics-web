apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: bbmetrics
  namespace: bbmetrics
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: bbmetrics
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
